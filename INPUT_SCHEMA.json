{
  "title": "Nexus Google Maps Scraper Input",
  "description": "Configuration for Nexus Google Maps Scraper",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrls": {
      "title": "Start URLs",
      "type": "array",
      "description": "URLs to start scraping from (e.g., https://www.google.com/maps/search/restaurants)",
      "editor": "requestListSources",
      "prefill": [
        {
          "url": "https://www.google.com/maps/search/restaurants"
        }
      ]
    },
    "maxRequestsPerCrawl": {
      "title": "Max Requests Per Crawl",
      "type": "integer",
      "description": "Maximum number of pages to scrape. Set 0 for unlimited.",
      "default": 100,
      "minimum": 0
    },
    "maxConcurrency": {
      "title": "Max Concurrency",
      "type": "integer",
      "description": "Maximum number of concurrent browser pages.",
      "default": 5,
      "minimum": 1,
      "maximum": 50
    },
    "proxyConfiguration": {
      "title": "Proxy Configuration",
      "type": "object",
      "description": "Proxy settings for the crawler.",
      "editor": "proxy",
      "default": {
        "useApifyProxy": true,
        "apifyProxyGroups": [
          "GOOGLE_SERP"
        ]
      }
    }
  },
  "required": [
    "startUrls"
  ]
}